import { h } from 'preact';
export default function Settings() {
  return (
    <div class="page settings">
      <h1>Settings</h1>
      <p>Node configuration, Fiber wallet, network selection.</p>
    </div>
  );
}
